name: hexcore
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.17.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.15
    
dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0

flutter:
  assets:
    - assets/certificate/riotgames.pem

platforms:
  windows: